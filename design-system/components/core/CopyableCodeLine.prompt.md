**CopyableCodeLine** — a monospace command / tailnet id / env key with a copy button. Advanced-surface only — keep raw commands, endpoints, and keys out of default views.

```jsx
<CopyableCodeLine label="Add an API key" code="hive-env-add GOOGLE_API_KEY" />
<CopyableCodeLine code="atlas.tail-fern.ts.net" />
```
