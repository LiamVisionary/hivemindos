{
  "name": "hivemindos-issues-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create hivemindos_issues",
    "d1:migrate:local": "wrangler d1 execute hivemindos_issues --local --file=./schema.sql",
    "d1:migrate:remote": "wrangler d1 execute hivemindos_issues --remote --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260521.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.17.0"
  }
}
