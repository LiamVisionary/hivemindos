# Changelog

This file records user-visible changes before they are committed. New work should
be added here first, then marked `Committed` or `Pushed` after the git action.

## Unreleased

- 2026-06-15 10:37:50 +0700 - Fix Windows/Linux Empty Fleet (Front-End Visibility Filter)
  - Status: Pushed (v0.2.15)
  - Areas changed: Fleet machine visibility filter (`src/features/fleet/fleet-identity.ts`), regression test + script wiring (`scripts/test-fleet-windows-visibility.mjs`, `package.json`)
  - Summary: A fresh Windows (or Linux) desktop still showed a completely empty Fleet view on v0.2.14 — no self machine cell and no add-agent cell — because the client-side `isVisibleFleetMachine` filter only kept Mac, mobile, or `hivemindos-*`-named machines. The local self device (`name: "This PC"`, `os: "win32"`/`"windows"`) matched no clause and was dropped from `machineGroups` after the v0.2.14 route-level fix already let it through, so `displayMachines` was empty and the add-agent hex (which lives inside the machine cluster) never rendered. The filter now always keeps `self` and adds a Windows/Linux desktop OS clause (matching both the `win32` HTTP-route spelling and the `windows` native-bridge spelling). The route-level `dedupeDevices` fix shipped in v0.2.14 stays in place; this closes the second, client-side layer it never covered.
  - Verification: `pnpm test:fleet-windows-visibility` passes, and was confirmed to FAIL against the pre-fix predicate (Windows self dropped) to prove the test reproduces the bug; `pnpm typecheck` clean on the changed file; `pnpm exec eslint src/features/fleet/fleet-identity.ts scripts/test-fleet-windows-visibility.mjs` passed. The test also includes a source-level drift guard asserting both route `dedupeDevices` keep `device.self` + `isDesktopDevice`, because the v0.2.14 route fix was silently dropped by a branch merge once already.
  - Intended commit message: `Fix Windows/Linux empty fleet: keep self in front-end visibility filter`

- 2026-06-14 17:37:22 +0700 - Add Agent Reach Guided Channel Setup Wizard
  - Status: Uncommitted
  - Areas changed: Agent Reach installable service backend/actions (`src/lib/services/installable-services.ts`, `src/app/api/fleet/apps/installable-services/route.ts`), Apps & Services Agent Reach setup UI (`src/features/dashboard/views/MyAppsPanel.tsx`, `src/features/dashboard/views/AgentReachSetupWizard.tsx`, `src/features/dashboard/agent-capability-catalog.ts`)
  - Summary: Adds a guided Agent Reach setup wizard so users can understand and configure all Agent Reach channel types instead of treating X as the only case. The wizard shows install/auth/test steps, an auth method picker, a clear Keychain/browser-cookie warning, per-channel status and setup guidance for public, browser-session, CLI-auth, and API-key channels, a data-flow view, a security receipt, an in-app X results viewer with JSON/Markdown export, and reset controls for the optional HivemindOS-managed X backend. The backend now supports explicit channel doctor refresh, X profile test fetches from an arbitrary profile URL/handle, and X backend reset while keeping credential-touching checks behind user actions.
  - Verification: `pnpm exec eslint src/lib/services/installable-services.ts src/app/api/fleet/apps/installable-services/route.ts src/features/dashboard/views/MyAppsPanel.tsx src/features/dashboard/views/AgentReachSetupWizard.tsx src/features/dashboard/agent-capability-catalog.ts --max-warnings=0` passed; focused `git diff --check` passed; filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <touched paths>` produced no diagnostics for the touched files. Live API smoke on `127.0.0.1:5021` returned Agent Reach wizard state with 13 channel rows, the security receipt, and the new actions (`agent-reach-doctor`, `test-agent-reach-x-profile`, `reset-agent-reach-x`). `agent-reach-doctor` through the app API returned Twitter/X `status: ok` with active backend `twitter-cli` plus public/web channels. `test-agent-reach-x-profile` through the app API fetched 2 posts and 1 reply thread for `https://x.com/0xLiamVisionary` through the Agent Reach twitter-cli backend. In-app browser verification of `?view=my-apps` opened the Agent Reach setup wizard and confirmed the guided setup, auth method picker, reset control, security receipt, and multi-channel matrix render for X/Twitter, Reddit, YouTube, V2EX, and RSS.
  - Intended commit message: `Add Agent Reach guided channel setup wizard`

- 2026-06-14 17:24:34 +0700 - Add Full-Vault Lexical Brain Index
  - Status: Uncommitted
  - Areas changed: Shared Brain Memory recall, `hive-brain` CLI fallback, generated vault index setup/defaults, search quality benchmark, docs, whole-brain contract tests
  - Summary: Shared-brain full-vault recall now borrows QMD's low-dependency wins without embeddings: a generated `Operations/Brain Services/Full Vault Search Index.jsonl` ranks markdown with collection-aware BM25-style lexical scoring, supports lightweight filters like `collection:projects`, `path:...`, quoted phrases, and exclusions, then loads the selected source notes before answering. Typed Agent Memory remains the first tier; the generated index is now the documented default broad-recall path on fresh vaults, with setup seeding `Operations/Brain Services/Full Vault Search Index.md` as the user-visible status note and the generated JSONL index sized for large shared vaults.
  - Verification: `pnpm test:shared-brain-index` passed with a scratch vault and `hive-brain --no-api`; `pnpm benchmark:shared-brain-search` passed the deterministic quality fixture; `pnpm test:qmd-brain-service` passed; `pnpm test:vault-structure` passed after the GitHub Pages layout token guard was restored; focused ESLint over the new index service, shared-brain recall, index tests, benchmark, and vault seeder passed; focused `git diff --check` passed; filtered TypeScript for the touched shared-brain files produced no diagnostics. Benchmark on a markdown-only copy of the 303 MB shared vault indexed 25,995 notes in 9.2s into a 70.6 MB JSONL index; five representative full-vault queries improved from 2,285 ms median old recall to 118 ms median indexed recall, a 19.4x median speedup, with identical top-1 results for all queries. A live seven-query quality benchmark on the actual vault found no relevance regression: old and indexed search both hit Top-1 for six exact expected notes, both missed one ambiguous Bankr imported-source query, and cached indexed median latency was 27.75 ms versus 4,506.20 ms for the old `rg`-first baseline.
  - Intended commit message: `Add full-vault lexical brain index`

- 2026-06-14 14:10:11 +0700 - Add Brain Speed Badge To QMD
  - Status: Uncommitted
  - Areas changed: Brain Services QMD dashboard card, QMD static test
  - Summary: The QMD Brain Services module now carries a `Brain Speed++` badge on the overview card and detailed module surface so the local markdown search upgrade is visually called out.
  - Verification: `pnpm test:qmd-brain-service` passed; focused `git diff --check` passed; in-app browser smoke on `127.0.0.1:5021/?view=vault&vaultPanel=brain-services` showed the QMD overview card rendering the badge in the existing uppercase badge style as `BRAIN SPEED++`.
  - Intended commit message: `Add QMD brain speed badge`

- 2026-06-14 13:46:53 +0700 - Add QMD Brain Service Setup
  - Status: Uncommitted
  - Areas changed: QMD brain service backend/API, Brain Services dashboard module, shared vault defaults, docs/tests
  - Summary: Adds QMD as a first-class optional Brain Services module so users can install the local markdown search engine, index the shared Obsidian brain, refresh embeddings, and query BM25/vector/hybrid retrieval from the dashboard. The setup is designed as a one-click local install path while keeping QMD cache/index files out of the vault and storing only service status notes in `Operations/Brain Services`.
  - Verification: `pnpm test:qmd-brain-service` passed; `git diff --check` passed; focused ESLint over the new QMD service/routes/dashboard files completed with warnings only in the existing large dashboard file; filtered typecheck for QMD-touched paths produced no diagnostics. Live API smoke on `127.0.0.1:5021/api/brain/qmd/status` returned a clean missing-CLI status (`spawn qmd ENOENT`) instead of crashing. In-app browser smoke of `?view=vault&vaultPanel=brain-services` showed the Brain Services overview with the new QMD card and `Install QMD` action. Full `pnpm typecheck` is still blocked by unrelated existing generated/promo and Tauri build-output diagnostics; `pnpm test:vault-structure` is blocked before QMD assertions by an unrelated existing `docs/_layouts/default.html` token mismatch.
  - Intended commit message: `Add QMD brain service setup`

- 2026-06-14 13:41:53 +0700 - Add Agent Reach Optional X Search Setup Flow
  - Status: Uncommitted
  - Areas changed: installable service backend/API (`src/lib/services/installable-services.ts`, `src/app/api/fleet/apps/installable-services/route.ts`), Apps & Services provider card/catalog (`src/features/dashboard/views/MyAppsPanel.tsx`, `src/features/dashboard/agent-capability-catalog.ts`)
  - Summary: Agent Reach now treats Twitter/X as an explicit optional setup step instead of implying that the base install is enough. The Apps & Services card reports Agent Reach, `pipx`, the optional `twitter-cli` X backend, and X auth as separate preflight states. Users who want X searching can click `Enable X search` to install audited `twitter-cli` 0.8.5 from a pinned PyPI wheel hash with reviewed dependency constraints; this install does not read cookies or authenticate. A separate `Check X auth` action runs the credential-touching `twitter status --json` check only when the user explicitly asks, shows a confirmation before browser-cookie auth can trigger a macOS Keychain prompt, and keeps background status polling from scanning browser sessions.
  - Verification: `pnpm exec eslint src/lib/services/installable-services.ts src/app/api/fleet/apps/installable-services/route.ts src/features/dashboard/views/MyAppsPanel.tsx src/features/dashboard/agent-capability-catalog.ts --max-warnings=0` passed; focused `git diff --check` passed; filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <touched paths>` produced no diagnostics for the touched files. Live API smoke on `127.0.0.1:5021` reports installed Agent Reach with `x-search-backend` preflight `ok: false` and an `Enable X search` action; the new X action is rejected for non-Agent Reach services; `Check X auth` fails safely before the backend is installed; and the host still has no `twitter` binary after status/auth checks, confirming no implicit backend install. In-app browser verification of `?view=my-apps` shows the Agent Reach card with the optional X setup description and `Enable X search` button. End-to-end app-flow testing installed the pinned `twitter-cli` backend through `Enable X search`, verified `twitter --version` reports `0.8.5`, and confirmed the explicit `Check X auth` path is the only step that requests X credential access. After one-time user approval of the macOS Keychain Chrome Safe Storage prompt, the app card reported "X search is enabled and authenticated through twitter-cli"; `agent-reach doctor --json` reported Twitter/X `status: ok` with active backend `twitter-cli`; `twitter user-posts 0xLiamVisionary --max 10 --json` returned 10 posts; `twitter tweet <id> --max 25 --json` successfully fetched reply threads for the 4 recent posts with reply counts; `pipx runpip twitter-cli check` passed; `uvx pip-audit --path /Users/liam/.local/pipx/venvs/twitter-cli/lib/python3.14/site-packages` found no known vulnerabilities; and no Agent Reach or twitter process/listener remained afterward.
  - Intended commit message: `Add Agent Reach optional X search setup flow`

- 2026-06-14 13:07:09 +0700 - Add Agent Reach pipx Preflight Action
  - Status: Uncommitted
  - Areas changed: installable service backend/API (`src/lib/services/installable-services.ts`, `src/app/api/fleet/apps/installable-services/route.ts`), Apps & Services provider card (`src/features/dashboard/views/MyAppsPanel.tsx`)
  - Summary: Agent Reach no longer leaves users stuck when `pipx` is missing. The Apps & Services card can now advertise a first-class `Install pipx` preflight action, install or locate `pipx` for HivemindOS without running the Agent Reach installer itself, then refresh the card so the normal pinned Agent Reach install path unlocks. The Agent Reach install still uses the reviewed constrained `pipx` archive path and keeps credential/browser-cookie setup separate from installation.
  - Verification: `pnpm exec eslint src/lib/services/installable-services.ts src/app/api/fleet/apps/installable-services/route.ts src/features/dashboard/views/MyAppsPanel.tsx --max-warnings=0` passed; filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <touched paths>` produced no diagnostics for the touched files; focused `git diff --check` passed. Live API smoke on `127.0.0.1:5021` accepted `POST /api/fleet/apps/installable-services` with `{"id":"agent-reach","action":"install-pipx"}` and returned the installed Agent Reach service with `pipx` preflight `ok: true`. The in-app browser Apps & Services card refreshed to show Agent Reach installed from the pinned fork. Agent Reach usage smoke followed the generated skill routes: web/Jina Reader fetched `example.com`, RSS/feedparser from the isolated Agent Reach venv read `https://hnrss.org/frontpage`, and `agent-reach format xhs` formatted local JSON. No `~/.agent-reach` config/token files were created and no Agent Reach-related process or listener remained afterward.
  - Intended commit message: `Add Agent Reach pipx preflight action`

- 2026-06-14 11:40:00 +0700 - Live Models For Configured Catalog Providers + Faster Provider Status Sweep
  - Areas changed: provider catalog (`src/lib/config/provider-catalog.ts`), new live-models route (`src/app/api/providers/models/route.ts`), agent settings modal (`src/features/dashboard/views/chat/AgentSettingsModal.tsx`), runtime status sweep (`src/lib/services/runtime-integrations.ts`)
  - Status: Uncommitted
  - Summary: Fixes provider model-count issues. (1) Configured providers showed wrong/low counts — OpenAI "0 models", and Venice/UsePod only their Hermes-configured subset (e.g. 4/2) rather than the provider's full catalog. A new auth-gated `/api/providers/models` route fetches a provider's live `/models` using the hive-env key (never returned to the client): catalog entries carry a base URL + auth style (OpenAI/Anthropic/Groq/Gemini/Venice); Venice-style non-chat models (image/audio/embedding) are filtered out. UsePod is also fetched for the provider-level model count via its tokenized proxy (the shared `USEPOD_TOKEN`); per-agent token selection still lives in UsePod's own guided setup. The picker fetches these in parallel off the critical path (never blocking the status sweep) and prefers the full live list when it exceeds the configured subset, so tiles + dropdowns show the real catalog; configured providers with no models yet show "Key set". (OpenRouter is excluded — huge catalog, reached via Adaptive/inventory.) (2) The settings status sweep ran Bankr model discovery, Bankr access status, and LM Studio discovery sequentially on every refresh; these independent probes now run concurrently (`Promise.all`), so the sweep waits the slowest probe instead of their sum. (The remaining ~6.6s is the Hermes inventory python, inherent to the Hermes runtime.) (3) Gateways seed a single placeholder `defaultModel`, so tiles briefly read a misleading "1 model" before the sweep filled in the real count; gateway tiles now show "Loading models" while the sweep is in flight and snap to the real count once (Hive Fusion's stable count and the catalog "Key set" tiles are excluded).
  - Verification: `npx tsc --noEmit` reported 0 errors under `src/`; `npx eslint` clean on all touched files; `node scripts/test-fusion.mjs` still passes (36 assertions); the new route resolves on the dev server (HTTP 401 auth-gated, i.e. compiles + routes). Live `/models` fetch depends on outbound network to the provider; it degrades gracefully (route 502 → tile stays "Key set", user can Add model manually).
  - Intended commit message: `Fetch live models for configured catalog providers; parallelize provider status sweep`

- 2026-06-14 11:21:08 +0700 - Fix Queen Bee Voice Full-Window Animated Glow Performance
  - Status: Uncommitted
  - Areas changed: Queen Bee voice perimeter glow and activation cue (`src/features/queen-voice/QueenVoiceGlow.tsx`, `src/features/queen-voice/queen-voice.module.css`, `src/features/queen-voice/QueenBeeVoiceOverlay.tsx`), focused overlay E2E selector (`scripts/e2e-queen-voice-overlay.mjs`)
  - Summary: Fixes the Queen Bee voice chat glow so it stays anchored to the full app viewport, keeps a rounded path that follows the app chrome, stays vibrant, and animates as a slow breathing bloom instead of a traveling beam. The laggy JavaScript-mutated gradient and the later CSS mask/band/dash implementations have been replaced with one measured SVG rounded-rectangle path that updates only on resize; all glow layers share that same path with fixed stroke widths, so neither outer nor inner edges move and the breathing effect comes only from opacity, blur, and saturation changes. Opening Queen Bee voice chat now also plays the sci-fi activation ping from `/audio/sfx/scifi-ping.wav`.
  - Verification: `pnpm exec eslint src/features/queen-voice/QueenVoiceGlow.tsx src/features/queen-voice/QueenBeeVoiceOverlay.tsx scripts/e2e-queen-voice-overlay.mjs --max-warnings=0` passed; filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <queen voice paths>` produced no diagnostics for the touched files; `git diff --check -- src/features/queen-voice/QueenVoiceGlow.tsx src/features/queen-voice/queen-voice.module.css scripts/e2e-queen-voice-overlay.mjs` passed; `node --check scripts/e2e-queen-voice-overlay.mjs` passed; `file public/audio/sfx/scifi-ping.wav` confirmed the activation sound is a PCM WAV; source guard confirmed the glow no longer uses glow-owned `requestAnimationFrame`, SVG gradient attribute mutation, CSS masks, dash/beam animation, side-band edge elements, path-scale breathing, or stroke-width breathing; fixed-width fixture `/tmp/queen-glow-fixed-width-breath-fixture.png` confirmed `widthsChanged: false` and `breathChanged: true` across all three layers, with blur/opacity changing while stroke widths stay fixed. Live `scripts/e2e-queen-voice-overlay.mjs` reached the local dashboard lock but did not unlock, so it could not mount the real overlay in that browser context.
  - Intended commit message: `Fix Queen Bee voice full-window animated glow performance`

- 2026-06-14 11:14:03 +0700 - Add Agent Reach Installable Service
  - Status: Uncommitted
  - Areas changed: installable service backend/API (`src/lib/services/installable-services.ts`, `src/app/api/fleet/apps/installable-services/route.ts`), Apps & Services provider catalog/UI (`src/features/dashboard/agent-capability-catalog.ts`, `src/features/dashboard/views/MyAppsPanel.tsx`)
  - Summary: Adds the reviewed HivemindOS Agent Reach fork as a first-class installable provider in Apps & Services. The installer uses `pipx` against a pinned fork commit plus source-archive hash with reviewed dependency constraints, registers only the packaged Agent Reach skill, shows provenance and cookie-consent security notes in the provider card, and treats Agent Reach as a CLI capability layer rather than a start/stop daemon. HivemindOS intentionally does not run Agent Reach's broader installer/doctor during the card install, because the container audit confirmed that even safe mode performs public channel probes.
  - Verification: `pnpm exec eslint src/lib/services/installable-services.ts src/app/api/fleet/apps/installable-services/route.ts src/features/dashboard/agent-capability-catalog.ts src/features/dashboard/views/MyAppsPanel.tsx --max-warnings=0` passed; filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <touched paths>` produced no diagnostics for the touched files; focused `git diff --check` passed; dev-server smoke test on `127.0.0.1:5021` returned 6 installable services including `agent-reach` with the pinned fork install command and security notes. Container verification installed the pinned fork inside an ARM64 Python 3.12 container, resolved 16 packages, found 0 known vulnerabilities with `pip-audit`, and showed that `agent-reach install --safe` skips cookie/system/global installs but still performs 31 network connect attempts during doctor checks. A second hardened-container audit ran HivemindOS' exact constrained `pipx install --backend pip --pip-args "-c <reviewed constraints>"` plus `agent-reach skill --install`; it resolved the same 16 packages, `pip check` passed, `pip-audit` found 0 vulnerabilities, skill registration created 7 skill files, and the skill-registration step made 0 network `connect()` calls. The audited source archive SHA-256 (`b96da4b09b0960cfccbfa2d07bf2af343abcb92998c193ffdeb58c41f40bfb2c`) is now embedded in the package URL. Local host install through the HivemindOS service API succeeded after installing `pipx` via Homebrew; `agent-reach --version` reports `1.5.0`, host `pipx runpip agent-reach freeze` matches the audited constraints, `pip check` passes, the app status endpoint reports installed, and skills are registered under the existing OpenClaw/Claude skill directories without creating config/token files.
  - Intended commit message: `Add Agent Reach installable service`

- 2026-06-14 02:55:00 +0700 - Provider Catalog + Inline Key Onboarding
  - Status: Uncommitted
  - Areas changed: new provider catalog (`src/lib/config/provider-catalog.ts`), model picker controller (`src/features/dashboard/hooks/use-agent-settings-controller.tsx`), agent settings modal grid + setup chain (`src/features/dashboard/views/chat/AgentSettingsModal.tsx`), shared key-setup component (`src/features/dashboard/views/chat/MissingSharedEnvKeySetup.tsx`)
  - Summary: The chat model picker now always shows the full first-class provider catalog (OpenRouter, OpenAI, Anthropic, Groq, Gemini + the existing gateways) instead of hiding providers whose key isn't configured. Tiles are ordered configured-providers-first (keyed/keyless/virtual or carrying real inventory models), then A–Z, with the first 8 shown and the rest behind a subtle "view all ⌄" toggle (Adaptive stays pinned) — so the visible top-8 favors providers the user has actually set up. Pressing an unconfigured key-based provider shows the same inline hive-env key input Venice/Bankr already use (reused `MissingSharedEnvKeySetup`, not duplicated), which saves to the shared hive env via `/api/env`. Saving now also propagates the credential into Hermes' env store: auto-written when Hermes doesn't have it, and gated behind a "Hermes <Provider> key is configured — update with this one?" toggle (OFF by default) when it does. Provider key presence is detected from `GET /api/env`; the key-needed UI only activates once presence is known (no false prompts on load or if the env API is unreachable). Only the credential value is written to Hermes' env — `model.default`/gateway config is untouched.
  - Verification: `npx tsc --noEmit` reported 0 errors under `src/`; `npx eslint` clean on all touched files (incl. the `@ts-nocheck` modal, which eslint still parses); the new catalog was runtime-checked (`providerKeyEnv`, keyless/virtual/guided flags correct for all 10 entries); `node scripts/test-fusion.mjs` still passes (36 assertions); the Next dev server compiles (HTTP 200). Live click-through of the picker is pending user confirmation in the running desktop app (UI not drivable from here).
  - Intended commit message: `Show full provider catalog with inline hive-env key onboarding + Hermes propagation`

- 2026-06-14 02:05:00 +0700 - Add Hive Fusion Compound Model
  - Status: Uncommitted
  - Areas changed: new Fusion orchestrator (`src/lib/services/fusion/{types,catalog,prompts,client,orchestrator,route-stream}.ts`), chat route intercept (`src/app/api/chat/agent-runtime/route.ts`), provider/model registry (`src/lib/config/model-provider-gateways.ts`), agent profile types (`src/lib/types/agent-runtime.ts`), model picker (`src/features/dashboard/hooks/use-agent-settings-controller.tsx`), provider icons (`public/icons/runtimes/hive-fusion.svg`, `public/icons/runtimes/openrouter.svg`), test + loader (`scripts/test-fusion.mjs`, `scripts/lib/ts-relative-loader.mjs`), docs (`docs/features/runtimes-and-chat.md`)
  - Summary: Implements Hive Fusion, a native compound model. Selecting the `Hive Fusion` provider (available on any runtime) fans a prompt out to a panel of the configured providers (OpenRouter free-budget models, Venice, OpenAI, Groq, Bankr, or a local OpenAI-compatible server) in parallel, runs a judge model to extract consensus/contradictions/coverage/unique-insights/blind-spots, then streams a synthesizer answer grounded in that analysis. The panel auto-resolves from whichever provider keys are present and degrades gracefully to a low-diversity single-member panel; `agent.fusion` overrides participants/judge/synthesizer/mode (mirroring OpenRouter's custom-panel option). OpenRouter's hosted compound model is surfaced separately as the `openrouter/fusion` slug under the OpenRouter provider (served by the normal OpenRouter path). The chat send path is unchanged — the route detects `provider: "hive-fusion"` and delegates before the single-model preflight, emitting panel/judge progress as tool/reasoning events and the synthesizer as the streamed answer over the existing SSE contract. Fusion output is run through the same `proxyOutput`/`redactSecretText` security proxy as every other runtime path, streamed synthesis uses an idle (not fixed) timeout, and selecting Fusion never writes a Hermes/OpenClaw gateway model (client guard + server-side set-model guard).
  - Verification: `node scripts/test-fusion.mjs` passed (36 assertions across the happy path, partial/all-member failure, synth fallback incl. mid-stream interruption, single-success judge-skip, judge failure, hosted OpenRouter mode, and the judge-JSON parser); `npx tsc --noEmit` reported 0 errors under `src/`; `npx eslint` reported 0 problems on all touched files. A 4-dimension adversarial multi-agent review (correctness/stream-contract/integration/security) confirmed and fixed 9 issues (2 high: OpenRouter `plugins` body shape + output-proxy bypass) and dismissed 7 non-issues. The orchestrator runs offline in tests via injected provider callers plus a `.ts`-extension resolve hook.
  - Intended commit message: `Add Hive Fusion compound model (panel -> judge -> synthesize)`

- 2026-06-14 01:32:32 +0700 - Restore macOS Header Window Dragging
  - Status: Uncommitted
  - Areas changed: desktop dashboard header (`src/features/dashboard/views/DashboardHeader.tsx`), Tauri window permissions (`src-tauri/capabilities/default.json`)
  - Summary: Restores click-and-drag window movement from the custom HivemindOS header on macOS desktop builds. The app already uses Tauri's overlay title bar, so the web header now declares the native `data-tauri-drag-region` that Tauri requires, and the desktop capability allows the injected drag handler to start native window dragging.
  - Verification: `pnpm exec eslint src/features/dashboard/views/DashboardHeader.tsx --max-warnings=0` passed; `node -e` JSON parsing passed for `src-tauri/capabilities/default.json`; focused `git diff --check` passed for the touched header, capability, changelog, and assimilation log files.
  - Intended commit message: `Restore macOS header window dragging`

- 2026-06-14 01:02:03 +0700 - Brand Remaining macOS Background Services
  - Status: Uncommitted
  - Areas changed: shared macOS helper/signing utility (`scripts/hivemindos-background-helper.c`, `scripts/macos-background-helpers.sh`), collector/Link/Sync installer (`scripts/install-telemetry-collector.sh`, `scripts/build-hivemind-linkd.sh`), voice worker installer (`scripts/install-claw-backend.sh`), Tauri helper resource staging (`scripts/tauri-build.mjs`), uninstall mirrors (`uninstall.sh`), local LaunchAgent installs
  - Summary: Extends the signed background-item fix to the remaining HivemindOS services. Hivemind Link now signs `hivemind-linkd` as `com.hivemindos.linkd`, Syncthing launches through a signed `HivemindOS Sync` helper, and the Claw voice process has been renamed to the generic `com.hivemindos.voice-worker` LaunchAgent using a signed `HivemindOS Voice Worker` helper. Setup also retires the old `com.hivemindos.claw-voice-worker` and `com.omni-agent-hivemind.syncthing` plists so System Settings no longer has stale generic shell/Homebrew background rows from HivemindOS-managed services.
  - Verification: `bash -n` passed for the helper/signing, collector, Claw, Link build, and uninstall scripts; `node --check scripts/tauri-build.mjs` passed; the shared helper compiled with `cc -O2 -Wall -Wextra -Werror` and exec smoke tests passed; `plutil -lint` passed for Collector, Link, Sync, and Voice Worker LaunchAgents. Live launchd verification shows Collector uses `HivemindOS Collector`, Link uses signed `hivemind-linkd`, Sync uses `HivemindOS Sync`, and Voice uses `HivemindOS Voice Worker`; the old `com.hivemindos.claw-voice-worker` and `com.omni-agent-hivemind.syncthing` plists are gone. `codesign -dv` reports all four executables/helpers signed by `Developer ID Application: Rizzma, Inc. (L7XLLTV3X7)`. Collector `/health`, Link `/status`, and Syncthing `127.0.0.1:8384` listener checks passed. Focused `git diff --check` passed. `node scripts/check-file-sizes.mjs` still reports pre-existing oversized files; touched files are under 1500 lines.
  - Intended commit message: `Brand remaining macOS background services`

- 2026-06-14 00:42:04 +0700 - Brand macOS Collector Background Item
  - Status: Uncommitted
  - Areas changed: macOS telemetry collector helper and installer (`scripts/hivemindos-background-helper.c`, `scripts/macos-background-helpers.sh`, `scripts/install-telemetry-collector.sh`, `scripts/tauri-build.mjs`, `uninstall.sh`), local LaunchAgent install
  - Summary: Replaces the raw `node` LaunchAgent entry for the local telemetry collector with a signed `HivemindOS Collector` helper. On Macs with the Rizzma Developer ID certificate, setup now signs the helper as `Developer ID Application: Rizzma, Inc. (L7XLLTV3X7)` before launchd registers it, so System Settings can attribute the background item to HivemindOS/Rizzma instead of showing Homebrew Node as an unidentified developer. Tauri release prep also stages the helper as a macOS resource so notarized builds can ship the same branded background executable.
  - Verification: Helper compiled with `cc -O2 -Wall -Wextra -Werror` and correctly execs child commands; local installer rebuilt the LaunchAgent so `ProgramArguments[0]` is `/Users/liam/.hivemindos/bin/HivemindOS Collector`; `codesign -dv` reports `Developer ID Application: Rizzma, Inc. (L7XLLTV3X7)` with identifier `com.hivemindos.collector-helper`; `launchctl print gui/$(id -u)/com.agent-control-room.telemetry` shows the helper as the launchd program; `lsof` confirms the collector is listening on `127.0.0.1:8787`; `node --check scripts/tauri-build.mjs`, `bash -n scripts/install-telemetry-collector.sh uninstall.sh`, `plutil -lint ~/Library/LaunchAgents/com.agent-control-room.telemetry.plist`, and focused `git diff --check` passed. `spctl` still reports the local helper as unnotarized; notarization is covered by the normal signed release build path. `node scripts/check-file-sizes.mjs` still reports pre-existing oversized files, while touched files remain under 1500 lines.
  - Intended commit message: `Brand macOS collector background item`

- 2026-06-14 00:29:17 +0700 - Polish Queen Bee Voice Perimeter Glow
  - Status: Uncommitted
  - Areas changed: Queen Bee voice perimeter glow (`src/features/queen-voice/QueenVoiceGlow.tsx`, `src/features/queen-voice/queen-voice.module.css`), assimilation records
  - Summary: Refines the independent Queen Bee voice chat screen-edge glow to feel closer to the Apple Intelligence reference: the perimeter now renders SVG rounded-rectangle strokes with Gaussian blur filters instead of Chromium CSS masked borders, removing the hard vertical bands and letting the glow fade softly inward while still drifting through the Apple Intelligence palette.
  - Verification: `pnpm exec eslint src/features/queen-voice/QueenBeeVoiceOverlay.tsx src/features/queen-voice/QueenVoiceGlow.tsx --max-warnings=0` passed; `git diff --check -- src/features/queen-voice/QueenVoiceGlow.tsx src/features/queen-voice/queen-voice.module.css CHANGELOG.md ASSIMILATION_LOG.md ASSIMILATION_LOG.jsonl ASSIMILATION.json` passed; `verify_assimilation_manifest.py` passed; Playwright fixture screenshots at 1440x1000 and 390x844 verified the SVG stroke glow hugs the screen perimeter, fades inward smoothly, and no longer renders hard rectangular mask bands. The live managed dashboard on port 5020 was reachable but locked in the test browser context, so the overlay was visually verified in an isolated fixture rather than by unlocking the dashboard.
  - Intended commit message: `Polish Queen Bee voice perimeter glow`

- 2026-06-14 00:20:46 +0700 - Add Native Bankr Chat Actions
  - Status: Uncommitted
  - Areas changed: Bankr action service/API (`src/lib/services/bankr-actions.ts`, `src/app/api/bankr/actions/route.ts`), chat runtime action wiring (`src/app/api/chat/agent-runtime/route.ts`), Bankr capability briefing and discovery (`src/lib/services/chat/bankr-capability-context.ts`, `src/lib/services/context-index.ts`, `src/lib/services/crypto-capability-router.ts`, `src/lib/services/bankr-llm.ts`), focused Bankr test (`scripts/test-bankr-actions.mjs`, `package.json`)
  - Summary: Adds native HivemindOS Bankr chat actions for wallet portfolio reads, swaps, token launches, Polymarket, Hyperliquid, recurring Bankr automations, NFT actions, and Bankr Agent API jobs. Read-only Bankr requests can run directly, while actions that can spend, trade, launch, bet, open leverage, mutate NFTs, or schedule automations produce a reviewed `Bankr action ready` card and execute only after the user confirms.
  - Verification: `pnpm test:bankr-actions` passed, covering Bankr prompt classification, confirmation-card round trip, portfolio API calls, and Agent API job polling with mocked fetch/key data. Focused `pnpm exec eslint` passed for the Bankr service/API, chat runtime wiring, capability/context surfaces, and test script. Filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <Bankr touched paths>` produced no diagnostics for the touched files. `git diff --check` passed for the touched files and assimilation/changelog records. `pnpm check-sizes` still fails on the pre-existing oversized-file list, including the legacy `src/app/api/chat/agent-runtime/route.ts`; new Bankr files are under 1500 lines (`bankr-actions.ts` 373, `/api/bankr/actions/route.ts` 98, `test-bankr-actions.mjs` 101).
  - Intended commit message: `Add native Bankr chat actions`

- 2026-06-14 00:10:24 +0700 - Add Agent Provider Integrations And PRD Decomposition
  - Status: Uncommitted
  - Areas changed: runtime catalog/adapters (`src/lib/types/agent-runtime.ts`, `src/lib/services/runtime-adapters/*`, `src/lib/services/runtime-availability.ts`), provider/MCP/inbox/service APIs (`src/lib/services/external-agent-providers.ts`, `src/lib/services/mcp/catalog.ts`, `src/app/api/mcp/catalog/route.ts`, `src/lib/services/cloudflare/agentic-inbox-blueprint.ts`, `src/app/api/cloudflare/agentic-inbox/route.ts`, `src/lib/services/installable-services.ts`, `src/app/api/fleet/apps/installable-services/route.ts`), Apps & Services UI (`src/features/dashboard/agent-capability-catalog.ts`, `src/features/dashboard/views/MyAppsPanel.tsx`), Queen Bee PRD decomposition (`src/lib/services/queen-bee/prd-decomposition.ts`, `src/app/api/queen-bee/route.ts`), context index, tests, docs, assimilation records
  - Summary: Adds the first HivemindOS-native provider bridge for the external agent tools list: Browser Use, Awesome MCP Servers, Cloudflare Agentic Inbox, OpenHands, Aider, n8n, and Task Master-style PRD decomposition are now discoverable through the context index/provider catalog and backed by runnable setup/execution surfaces where HivemindOS owns the integration. OpenHands and Aider now expose real `run-task` background bridges with dashboard-visible run logs, shared-env credential loading, uv/Homebrew-aware CLI paths, and Apps & Services install cards for both CLIs. `/api/browser-use` and Apps & Services now install Browser Use from PyPI with `uv`, without invoking surprise privileged browser setup, and expose version/provenance/security notes in the provider card. Browser Use commands launched by HivemindOS disable anonymized telemetry, constrain URL opening to `http`/`https`/`about:blank`, constrain screenshots to the HivemindOS run directory, and keep setup/install plus direct cookie/profile/recording/tunnel/python management outside the runtime bridge. A full-permissions toggle on the Browser Use provider card opens the shared slide-to-unlock warning and persists the machine-level permission server-side, unlocking Browser Use Cloud task creation, real-profile/CDP launch options, uploads, and JavaScript eval without per-action hand-holding. `/api/cloudflare/agentic-inbox` now reports setup status, scaffolds a real Cloudflare Worker inbox project with `npx wrangler` scripts that work before a local npm install, sanitizes Wrangler status output, and can deploy it with Wrangler after Cloudflare prerequisites are configured. `/api/mcp/catalog` exposes a curated MCP catalog with credential key names and side-effect notes. Apps & Services can install/start/stop n8n as a local Docker-backed service on `127.0.0.1:5678` with localhost-safe HTTP settings and blocks the install button when Docker is installed but the daemon is unavailable. Queen Bee can parse PRDs into an epic plus linked Work Board tasks with acceptance criteria and dependency hints via `action: "decompose-prd"`.
  - Verification: `pnpm test:agent-providers` passed, now covering executable OpenHands/Aider bridges, Browser Use install/executor surfaces and guardrails, Agentic Inbox setup/deploy scaffolding, n8n localhost-safe Docker settings, the provider matrix, MCP catalog search, Agentic Inbox blueprint, and Queen Bee PRD route/source contract. In the live Apps & Services view on the managed `5020` app, Browser Use installed/started/stopped through the UI and ended with `No active sessions`; its card now shows `browser-use[cli] v0.13.1`, the latest-compatible unpinned PyPI update policy, security notes, and the Browser Use full-permissions toggle. Live `/api/browser-use` calls reject `eval` while full permissions are off, then allow the same `eval` call after full permissions are enabled (`result: 2`); `file://` URLs remain rejected. The full-permission Browser Use runtime opened a local fixture page, executed JavaScript to click a button, read back `status: clicked 1`, and saved `/Users/liam/.hivemindos/runtime-runs/browser-use/browser-use-fixture-clicked.png`. The live Browser Use card disables full permissions from `Full permissions on`, opens the warning modal when re-enabling, and the slide-to-unlock control flips the card back to `Full permissions on` with the server-side API reporting `fullAccess: true`. The bulk installable-services endpoint returns all five provider statuses, and the live Apps & Services view renders Browser Use, OpenHands, Aider, n8n, and Agentic Inbox states from that status payload. Aider installed through the in-app provider card and refreshed to a disabled `Installed` button; the HivemindOS Aider runtime bridge ran `aider-mqcoz5cw` against `/tmp/hive-runtime-aider`, loaded `OPENAI_API_KEY` from shared hive env, completed, and edited `math.js` to export `double(n)`. OpenHands installed through the in-app provider card and refreshed to a disabled `Installed` button; after adding `--override-with-envs` plus `LLM_API_KEY`/`LLM_MODEL` env mapping, the HivemindOS OpenHands runtime bridge ran `openhands-mqcp33aj` against `/tmp/hive-runtime-openhands`, initialized `gpt-4o-mini`, edited `project.txt`, and completed with a verified diff. n8n install was attempted through the service API and is correctly blocked with `Docker is required before HivemindOS can install n8n as a local service`; Agentic Inbox setup scaffolded the Worker project, `npm run check` now runs `npx wrangler deploy --dry-run` successfully, and deploy was not clicked because it would publish Cloudflare resources/email routing. `/api/mcp/catalog?q=github%20linear%20notion` returned the curated GitHub MCP server entry with credential and safety metadata. Queen Bee PRD decomposition preview returned a task draft, and a real create run against `/tmp/hive-queen-vault` created one epic plus five linked Work Board tasks without touching the shared vault. Focused `pnpm exec eslint` passed for the touched Browser Use runner, installable-service, runtime adapter, shared-env, Apps UI, route, and provider-test files. Filtered `pnpm exec tsc --noEmit --pretty false --skipLibCheck | rg <touched paths>` produced no diagnostics for the touched files. `git diff --check` passed. Touched large files remain under the 1500-line cap (`MyAppsPanel.tsx` 1246, `agent-runtime.ts` 1334, `context-index.ts` 1323). `pnpm check-sizes` still fails on pre-existing oversized files such as `scripts/agent-telemetry-collector.mjs`, `src/app/api/chat/agent-runtime/route.ts`, `src/features/dashboard/DashboardApp.tsx`, `src/app/kanban-board.module.css`, and others.
  - Intended commit message: `Add agent provider integrations and PRD decomposition`

- 2026-06-13 22:30:18 +0700 - Document Compiled Brain Search Performance
  - Status: Uncommitted
  - Areas changed: public GitHub Pages docs (`docs/index.md`, `docs/features/index.md`, `docs/features/brain-vault-and-skills.md`, `docs/whole-brain/index.md`, `docs/whole-brain/brain-services.md`, `docs/packaged-skills/hive-skills.md`), top-level README, vault structure guard, assimilation records
  - Summary: Updates the user-facing docs for the new compiled-brain search flow. The GitHub Pages guide now describes Compiled Knowledge as a fast retrieval lane for reviewed entity/concept/summary wiki pages, shows a product-style retrieval snapshot from the deterministic 720-page benchmark, and explains when agents should use `brain_search_knowledge` instead of broad shared-brain recall.
  - Verification: `ruby -rjekyll -e 'Jekyll::Commands::Build.process({"source"=>"docs","destination"=>"/tmp/hivemindos-docs-compiled-brain"})'` passed; generated HTML contains `Compiled brain search`, `Compiled Brain Retrieval`, `Compiled Retrieval Snapshot`, `brain_search_knowledge`, and the headline retrieval timings. `rg` confirmed the new docs/README source strings, and `git diff --check` passed. `pnpm test:vault-structure` still fails on the pre-existing docs layout assertion (`docs/_layouts/default.html` missing `/whole-brain/`), not on the new compiled-brain docs guards.
  - Intended commit message: `Document compiled brain search performance`

- 2026-06-13 22:00:59 +0700 - Quiet Loading Screen Glow
  - Status: Uncommitted
  - Areas changed: dashboard loading overlay component/styles, route placeholders, and Brain graph contrast (`src/features/dashboard/DashboardHiveLoader.tsx`, `src/features/dashboard/DashboardHiveLoader.module.css`, `src/features/dashboard/dashboard-display-helpers.tsx`, `src/app/DashboardNativeFrame.tsx`, `src/app/vault.module.css`), Fleet hive backdrop (`src/components/fleet/FleetView.tsx`), Next route loading fallback (`src/app/loading.tsx`), Tauri static startup loading page (`src-tauri/loading/index.html`), assimilation records
  - Summary: Removes the warm yellow-green bloom behind the HivemindOS loading mark and fully flattens the Fleet hive stage backdrop so route transitions, desktop startup, and the live swarm map feel sharper and less like compressed background imagery. The dashboard hive loader and route placeholders now use a smaller comb mark, tighter spacing, flatter dark overlays, and quieter progress rails, and the Brain graph keeps node labels readable on its dark canvas even when the dashboard root is in light theme.
  - Verification: `pnpm exec eslint src/app/loading.tsx src/app/DashboardNativeFrame.tsx src/features/dashboard/dashboard-display-helpers.tsx src/features/dashboard/DashboardHiveLoader.tsx src/components/fleet/FleetView.tsx src/features/dashboard/views/BrainGraphExplorer.tsx --max-warnings=0` passed; `git diff --check` passed for the touched loader/Fleet/changelog/assimilation files; touched file line counts are under 1500 (`vault.module.css` now 1411 lines after extracting loader styles); source guard confirms Fleet and loader backdrops no longer use radial gradients. Static Tauri startup loader rendered via Playwright from `src-tauri/loading/index.html`; live dashboard screenshot was blocked by the local dashboard lock, so auth-protected route/Fleet visuals were verified by source guard and a local fixture without reading the dashboard token.
  - Intended commit message: `Quiet loading screen glow`

- 2026-06-13 21:48:56 +0700 - Add Compiled Brain Wiki And Graph Tools
  - Status: Uncommitted
  - Areas changed: compiled knowledge service/API (`src/lib/services/obsidian/compiled-knowledge.ts`, `src/app/api/brain/knowledge/route.ts`), shared contribution policy (`src/lib/services/brain/shared-contribution-contract.ts`), external MCP tools (`scripts/hivemind-mcp`), runtime shared-vault prompt context (`src/lib/services/chat/shared-vault-context.ts`), context index discovery (`src/lib/services/context-index.ts`), packaged/live shared skill (`packaged-skills/auto-install/hive-brain-compiled-wiki/SKILL.md`, shared vault `Skills/hive-brain-compiled-wiki/SKILL.md`), setup/initializer instructions (`scripts/seed-shared-skills.sh`, `setup.ps1`, `scripts/seed-vault-foundation.mjs`, `AGENTS.md`, shared vault `AGENTS.md`), whole-brain/product docs, vault structure guard, focused fixture and benchmark tests (`scripts/test-compiled-knowledge.mjs`, `scripts/benchmark-compiled-knowledge.mjs`, `package.json`), assimilation records
  - Summary: Adds a HivemindOS-native compiled-brain workflow inspired by The Curator. Agents can now compile important source material, research findings, and chat conclusions into Obsidian entity/concept/summary wiki pages under `Synthesis/Compiled Knowledge/<domain>/`, search that compiled wiki with weighted slug/title/tag/body matching, query it as a graph, inspect backlinks, scan wiki health, apply safe repairs, and consult a shared-brain contribution contract. Human collective `shared-*` mirrors follow the personal-domain-then-synthesis model, while normal agent-to-agent HivemindOS contributions remain permissive unless a domain is explicitly read-only.
  - Verification: `pnpm test:compiled-knowledge` passed and covers compiled wiki writes, compiled search, graph overview/full graph reads, node/backlink lookup, health scan/dismissal, MCP tool exposure, packaged skill discovery, and the shared contribution contract (`human-collective` protects `shared-*`; `agent-to-agent` stays permissive). `pnpm benchmark:compiled-knowledge` passed on a synthetic 720-page compiled-knowledge vault across 15 runs: old generic graph median 136.48ms with 271,902-byte payload and only 260 real notes/520 links before truncation; new compiled overview median 39.45ms with 2,851-byte payload while covering 720 notes/1,440 edges (3.46x faster, 95.37x smaller, 2.77x more real-note and edge coverage); new full compiled graph median 36.56ms while returning all 720 notes/1,440 edges. Direct slug lookup for `concept-120` improved from broad-graph scan median 135.81ms/271,902 bytes to compiled node lookup median 31.71ms/392 bytes (4.28x faster, 693.63x smaller). Backlink lookup improved from broad-graph filter median 87.25ms/271,902 bytes and 0 backlinks because of truncation to compiled backlink lookup median 32.44ms/80 bytes and 2 backlinks (2.69x faster, 3,398.78x smaller, more complete). New compiled search for `Concept 120 benchmark graph` improved from broad vault rg markdown search median 128.67ms/3,465 bytes to compiled search median 67.18ms/3,377 bytes with the same top target (`concept-120`), giving 1.92x faster search and a 1.03x smaller payload. Health scan has no old generic equivalent; the new compiled health scan completed at median 62.47ms with a 220-byte clean-health payload. Live shared-vault discovery was refreshed with `./scripts/seed-shared-skills.sh --import-sources none --share-targets none`; `rg` found `brain_search_knowledge` in `Skills/README.md`, `Skills/hive-brain-compiled-wiki/SKILL.md`, and shared vault `AGENTS.md`; `hive-brain recall "hive-brain-compiled-wiki compile_brain_knowledge brain_get_backlinks" --scope full-vault --limit 8` returned `Hive Brain Compiled Wiki` as the top hit; and `node scripts/hivemind-mcp` advertised `brain_search_knowledge` plus the existing compiled-brain tools. `node --check scripts/hivemind-mcp scripts/test-compiled-knowledge.mjs scripts/benchmark-compiled-knowledge.mjs` passed; focused `pnpm exec eslint` passed for the touched compiled-brain service, API route, context index, shared-vault context, and scripts; filtered `tsc --noEmit --skipLibCheck` output has no diagnostics for the touched compiled-search files; `git diff --check` passed. `node scripts/check-file-sizes.mjs` still fails on pre-existing oversized files, and the touched files are under 1500 lines. `pnpm test:vault-structure` is still blocked by an existing docs layout assertion (`docs/_layouts/default.html` missing `/whole-brain/`).
  - Intended commit message: `Add compiled brain wiki and graph tools`

- 2026-06-13 18:45:51 +0700 - Use HivemindOS App Icon In Docs Header
  - Status: Uncommitted
  - Areas changed: GitHub Pages brand asset (`docs/assets/img/app-icon-1024.png`), docs layout favicon/header (`docs/_layouts/default.html`), docs stylesheet (`docs/assets/css/site.css`), assimilation records
  - Summary: Replaces the temporary text `H` badge in the GitHub Pages docs header with the HivemindOS app icon and wires the same image as the docs favicon/apple-touch icon, so the public docs now carry the real product identity.
  - Verification: `jekyll build --source docs --destination /tmp/hivemindos-docs-root/hivemindos` passed; browser preview refreshed at `http://127.0.0.1:6127/hivemindos/product/design-philosophy.html`; Playwright confirmed the brand image resolves to `/hivemindos/assets/img/app-icon-1024.png`.
  - Intended commit message: `Use HivemindOS app icon in docs header`

- 2026-06-13 18:33:03 +0700 - Improve GitHub Pages Docs Navigation
  - Status: Uncommitted
  - Areas changed: GitHub Pages layout (`docs/_layouts/default.html`), Pages stylesheet (`docs/assets/css/site.css`), nested docs navigation data (`docs/_data/navigation.yml`), Jekyll front matter across public docs markdown, assimilation records
  - Summary: Refreshes the GitHub Pages documentation into a cleaner GitBook-style reading experience. The docs now use a white reading canvas, sticky top bar, search/filter field, copy-link button, active-page highlighting, collapsible nested directory sections, and generated previous/next cards. Previously raw-copied Markdown docs now receive Jekyll front matter so sidebar entries resolve to styled HTML pages instead of broken or unstyled markdown paths.
  - Verification: `jekyll build --source docs --destination /tmp/hivemindos-docs-root/hivemindos` passed; generated nav-target check reports all sidebar targets exist; Playwright desktop/mobile screenshots verified layout at `http://127.0.0.1:6127/hivemindos/`; child-page check verified active nav (`Fleet`), previous/next cards (`Overview`, `Agents And Chat`), and sidebar filtering from 42 visible links to 1 for `wallet`; `git diff --check -- docs CHANGELOG.md ASSIMILATION_LOG.md ASSIMILATION_LOG.jsonl ASSIMILATION.json` passed. `node scripts/check-file-sizes.mjs` still fails on pre-existing oversized non-doc app files; touched docs shell files are under 1500 lines (`default.html` 212, `site.css` 1007, `navigation.yml` 156).
  - Intended commit message: `Improve GitHub Pages docs navigation`

- 2026-06-13 18:10:36 +0700 - Split Agent Soul From Suited-For Profiles
  - Status: Uncommitted
  - Areas changed: bee worker soul/profile types and defaults, agent settings modal, saved SOUL.md API/store, runtime agent creation/import, runtime prompt and phone-call context, bee soul regression test, assimilation log
  - Summary: Agent worker classes now keep identity in `Soul` and task-fit guidance in `Suited for`. The settings modal lets users mix and match subclass defaults, imported `SOUL.md` files, and reusable saved souls without overwriting an existing default profile file: users can import a local SOUL.md, save the current soul as a new reusable SOUL.md, reset to the selected subclass soul, or load one from their saved soul library. Runtime creation and existing Hermes profile import now store SOUL.md content in `soulPrompt`, while `skillProfilePrompt` stays focused on what the agent is best suited to do. The shared system prompt and phone-call context combine both fields so the behavior works across runtimes, not only Hermes.
  - Verification: `node --check scripts/agent-telemetry-collector.mjs` passed; `node scripts/test-bee-worker-souls.mjs` passed; focused `pnpm exec eslint` passed for the new saved-souls service/API, agent settings/controller files, runtime prompt, phone-call context, and bee soul test; `git diff --check` passed for touched files. Full `pnpm exec tsc --noEmit --pretty false` is still blocked by pre-existing unrelated diagnostics under `promo-videos/code/HivemindOS-21`, the Remotion `pathLength` issue, and stale `src-tauri/target/release/resources` bee-worker copies.
  - Intended commit message: `Split agent soul from suited-for profile`

- 2026-06-13 16:00:21 +0700 - Restore Dashboard Auth-Denied Unlock Redirect
  - Status: Uncommitted
  - Areas changed: dashboard hydration loader (`src/lib/services/dashboard-state-client.ts`), snapshot regression test (`scripts/test-dashboard-state-snapshot.mjs`)
  - Summary: Reconciles the two divergent hydration fixes that collided during the buy-stock merge. The committed loading-screen version (native desktop read, retry-forever, `onRetry` telemetry) is kept, and the previously-dropped auth handling from the Brain empty-boot fix is layered back in: a `/api/dashboard/state` 401 now bounces to the unlock flow (`redirectToDashboardUnlock`, or a `DashboardStateAuthError` on the server) instead of resolving `{}`, which would hydrate a fake-empty dashboard and mask an expired session. A legitimately empty store (200, `values {}`) still resolves and caches, kept distinct from auth denial. Also repairs the snapshot regression test, which could not load the source after a top-level native import was added (the vm harness now strips imports and mocks `isTauriDesktopRuntime`), and updates it to assert the 401-redirect behavior plus the empty-store caching case.
  - Verification: `pnpm test:dashboard-state-snapshot` passes (4 cases: outage→recovery, 503 retried, 401→`DashboardStateAuthError`, empty-store cached); `eslint` clean on both files; `tsc --noEmit` reports no diagnostics in either file (full run remains blocked only by the pre-existing unrelated Remotion `pathLength` error and stale `src-tauri/target/release/resources` copies). The browser redirect path itself (window present) needs the running app for a live look.
  - Intended commit message: `Restore dashboard 401 unlock redirect; repair snapshot test`

- 2026-06-13 15:49:14 +0700 - Add OKF Export For Shared Brain Memory
  - Status: Uncommitted
  - Areas changed: OKF export/validation service (`src/lib/services/obsidian/okf.ts`), OKF API route (`src/app/api/brain/okf/route.ts`), focused OKF export test (`scripts/test-okf-export.mjs`, `package.json`), README and GitHub Pages docs (`README.md`, `docs/index.md`, `docs/features/index.md`, `docs/whole-brain/index.md`, `docs/whole-brain/brain-services.md`, `docs/whole-brain/code-map.md`, `docs/features/brain-vault-and-skills.md`), vault structure contract (`scripts/test-vault-structure-contract.mjs`), assimilation log
  - Summary: Adds an Open Knowledge Format v0.1 exchange surface for the shared brain. HivemindOS can now export Agent Memory and conversation mirrors into a clean OKF bundle with concept markdown files, YAML frontmatter, generated `index.md` and `log.md`, and a validator that enforces OKF's required frontmatter plus non-empty `type` rule while reporting optional discovery metadata as warnings. The native Obsidian vault remains the source of truth; OKF is an interoperability bundle for outside agents and catalog tools.
  - Verification: `pnpm exec eslint src/lib/services/obsidian/okf.ts src/app/api/brain/okf/route.ts scripts/test-okf-export.mjs --max-warnings=0` passed; `pnpm test:okf-export` passed; `pnpm test:vault-structure` passed; filtered `pnpm exec tsc --noEmit --pretty false` returned no diagnostics for the OKF service, route, or test script; full `pnpm exec tsc --noEmit --pretty false` remains blocked by pre-existing unrelated diagnostics in `remotion/gitlawb-integration-video/src/GitLawbIntegrationVideo.tsx` (`pathLength`) and stale Tauri release resources missing the `security` bee worker class. Live API smoke on temporary dev server `http://127.0.0.1:5021` exported 12 Agent Memory concepts to `Operations/Brain Services/OKF Export` and validation returned `ok: true`, `errors: 0`, `warnings: 0`; direct validation GET returned the same. Generated OKF export was scanned for raw IP-looking strings and obvious secret tokens; hits were proof hashes, credential key names, and loopback/local references, not raw Tailnet IPs or plaintext secret values. `git diff --check` passed for touched files.
  - Intended commit message: `Add OKF export and validation for shared brain`

- 2026-06-14 03:30:00 +0700 - Fix Windows/Linux machines missing from Fleet + add boot-failure recovery
  - Status: Uncommitted
  - Areas changed: tailnet device filter (`src/app/api/tailscale/devices/route.ts`, `src/app/api/fleet/discover/route.ts`), client self re-injection (`src/features/dashboard/DashboardApp.tsx`), interactive boot-error screen + retry + 60s timeout (`src-tauri/src/lib.rs`, `src-tauri/loading-shell/index.html`, `src-tauri/build.rs`, `src-tauri/capabilities/default.json`)
  - Summary: ROOT CAUSE for a Windows desktop showing a completely empty Fleet (no machine cell, no add-agent cell): the fleet device list filtered devices to mac/mobile/hivemind-link only (`dedupeDevices` in both the tailscale-devices and fleet-discover routes), so a Windows or Linux machine — including the user's own self device — was dropped entirely, and any agents on it had no machine cell to render in. Both routes now keep Windows/Linux desktops and never drop a machine's own `self` device. A client-side safety net also re-injects the native self device if any upstream source omits it. SEPARATELY, boot resilience: when the embedded server fails to start, the window now shows an interactive error screen (server-log tail + one-click Copy + a real Retry that re-spawns the server without restarting the app — the old "rerun setup" did nothing), and the failure is reported to telemetry from the webview (the static error page reported nothing, which is why these failures were invisible). The first-boot wait is raised 25s -> 60s for slow Defender-scanned Windows first launches.
  - Verification: `cargo check --release`; `pnpm check:tauri-acl` (38 commands in lockstep); `pnpm exec eslint` clean on changed TS. Live confirmation pending on the affected Windows machine.
  - Intended commit message: `Fix Windows/Linux fleet visibility + add boot-failure recovery screen`

- 2026-06-14 02:10:00 +0700 - Fix empty Fleet view on a fresh install (self machine never showed)
  - Status: Uncommitted
  - Areas changed: tailnet device handling on the dashboard (`src/features/dashboard/DashboardApp.tsx` `refreshTailscaleDevices`)
  - Summary: Fixes a brand-new install (e.g. Windows, no Tailscale, no agents) showing an empty Fleet view — no "This Mac" self cell and no "add agent" cell, just a perpetual loading swarm with no way to onboard a first agent. The native fleet read correctly returns the local self device, but the client's `localOnlyStatusFallback` guard (meant to avoid clobbering an existing multi-machine fleet during a momentary Tailscale outage) was discarding that self device on a fresh machine that has no Tailscale at all. With zero machines the hive renders only its loading skeleton, and the add-agent cell never appears because it lives inside a machine cluster. Now the guard only suppresses the self-only update when a fleet already exists; on a fresh box it surfaces the self machine, so the Fleet view always shows "This Mac" plus the add-agent affordance.
  - Verification: traced the full render path (FleetView -> NetworkGraph -> MachineCluster add-hex-cell; machineGroups -> fleetViewData filter, which passes any `machine.self`); confirmed the self device passes the filter once set. `pnpm exec eslint` on the changed file. Live fresh-install fleet to be confirmed on the affected Windows machine after the build.
  - Intended commit message: `Fix empty fleet on fresh install: surface self machine when no prior fleet`

- 2026-06-14 01:30:00 +0700 - Fix Windows: embedded server never started (EISDIR on drive root)
  - Status: Uncommitted
  - Areas changed: Node spawn flag in the embedded boot (`src-tauri/src/lib.rs` `start_native_next_server`); removed the one-off `\.github/workflows/debug-windows-server-boot.yml` diagnostic
  - Summary: Fixes the Windows-only "HivemindOS couldn't start its local server" error that hit every Windows install since the first embedded release (v0.2.10). The Tauri runtime hands Node the embedded server path in Windows extended-length (verbatim) form (`\\?\C:\...\server.js`); Node realpath-resolves the main module at startup and crashes on that form with `EISDIR: illegal operation on a directory, lstat 'C:'`, so the dashboard backend never booted and every launch (and "rerun setup") landed on the error page. The fix launches Node with `--preserve-symlinks-main`, which skips the realpath of the entry script while keeping the long-path-safe prefix. macOS/Linux are unaffected.
  - Verification: Reproduced AND fix-validated on a windows-latest runner: the verbatim path crashes with the exact user error, and the same path with `--preserve-symlinks-main` boots and serves (SSR + /api). `cargo check --release` passes. NOTE: a fully-broken Windows install can't auto-update (the updater runs in the dashboard, which never loaded), so affected users must download the fixed build once.
  - Intended commit message: `Fix Windows embedded server boot: --preserve-symlinks-main (EISDIR on \\?\ path)`

- 2026-06-14 00:30:00 +0700 - Instant-paint launch for the embedded desktop app
  - Status: Uncommitted
  - Areas changed: async embedded boot + navigate-on-ready (`src-tauri/src/lib.rs`), branded loading shell (`src-tauri/loading-shell/index.html`), ship shell as embedded frontendDist (`scripts/tauri-build.mjs`), expose the Tauri bridge to the shell (`src-tauri/tauri.conf.json` `withGlobalTauri`)
  - Summary: The desktop app now paints instantly at launch instead of showing a frozen blank window for 1-3s while its local server boots. Previously the embedded build spawned the bundled Node server synchronously inside Tauri's setup() on the main thread, freezing the whole app (no paint) until the server's port opened. Now the server boots on a background thread and the window immediately shows a branded HivemindOS loading screen served from a local file — which reads live data from the native Rust commands (This Mac + how many tailnet machines are online) so the first thing you see is real, not just a spinner. When the server is ready the window swaps to the full dashboard automatically; if it never comes up, the existing local-server error page is shown. Everything degrades gracefully: if a native read is unavailable the stat is hidden and the branded loader still shows.
  - Verification: `cargo check --release` (the boot path is release-only); `node --check scripts/tauri-build.mjs`; `pnpm check:tauri-acl` (36 commands in lockstep); JSON config validated. True first-paint timing is a GUI behavior — to be confirmed on a desktop build/launch (the shell + async boot are structurally verified in the bundle).
  - Intended commit message: `Instant-paint launch: async embedded boot + native loading shell`

- 2026-06-14 00:05:00 +0700 - Document landing-page / release-asset contract
  - Status: Uncommitted
  - Areas changed: new `LANDING_PAGE.md`, pointer section in `AGENTS.md`
  - Summary: Records how the separate landing-site repo consumes this repo's GitHub releases — download cards use version-independent `releases/latest/download/<stable-asset>` URLs, so a new Latest release is served automatically. Documents that the stable installer filenames (`HivemindOS-macos-apple-silicon.dmg`, etc.) are a contract shared by the release workflow's "Collect bundle assets" step, `scripts/build-updater-manifest.mjs`, and the landing page: renaming an asset silently breaks both downloads and the auto-updater. Doc-only housekeeping.
  - Verification: doc-only; no code or build affected.
  - Intended commit message: `Document landing-page / release-asset contract`

- 2026-06-13 14:49:41 +0700 - Add Buy-Stock Rail (Alpaca + On-Chain xStocks)
  - Status: Uncommitted
  - Areas changed: new buy-stock service (`src/lib/services/trading/buy-stock.ts`), verified xStocks mint allowlist (`src/lib/config/xstocks-tokens.ts`), wallet types + spend ledger (`src/lib/types/agent-wallet.ts`, `src/lib/services/wallet/spend-ledger.ts`), agent chat runtime wiring (`src/app/api/chat/agent-runtime/route.ts`), wallet config UI (`src/components/wallet/AgentWalletCard.tsx`), tests (`scripts/test-buy-stock-xstocks.mjs`, `scripts/e2e-buy-stock-alpaca.mjs`, `scripts/e2e-buy-stock-xstocks-swap.mjs`, `package.json`), docs (`docs/features/wallets-honey-and-x402.md`, `docs/features/index.md`)
  - Summary: Adds a unified "buy a stock from a prompt" rail with two venues behind one tool. `alpaca` places market orders through the Alpaca Trading API (paper by default; live only when the wallet sets `alpacaPaper:false`; keys read from shared hive env by name, never stored in project files). `xstocks` buys on-chain tokenized equities by swapping USDC into a verified xStock SPL token via Jupiter, signed by the agent's existing local Solana wallet. Tickers resolve only through a hard-coded Jupiter-verified mint allowlist (every mint carries the official `Xs` vanity prefix) so Solana symbol-squatting copycats cannot be bought. The flow reuses the existing draft -> confirm -> execute chat pattern ("buy $25 of AAPL on xstocks" -> Buy stock ready card -> reply `confirm`), a per-trade USD cap (`maxTradeUsd`), and the shared spend-governance chokepoint + ledger (a new `trade` spend kind), so kill switches, rolling budgets, and approval escalation all apply. The Wallets tab gets a Stock buying section (venue, Alpaca paper/live, max per trade).
  - Verification: `tsc` and `eslint` clean on all new/changed source files (the only remaining `tsc` errors are pre-existing and unrelated: a Remotion `pathLength` error and stale copies under `src-tauri/target/release/resources/`). `pnpm test:buy-stock` passed (14/14 verified xStock mints structurally sound and live-routable on Jupiter). On-chain xStocks E2E verified with real funds: a $1.00 USDC -> 0.00343601 AAPLx swap confirmed on Solana mainnet (tx `4QHtuZza1jikX25xkbNNdpHTP5Y8C2YDExCU8yvmK4CESwuXcagwN1HEEqpAPoj134PeS2GTypWAqdJiCJYHrNN`), with before/after balances read back to prove settlement. Alpaca paper E2E (`pnpm e2e:buy-stock-alpaca`) is written and ready but not yet run: no `ALPACA_*` keys are present in the shared hive env.
  - Intended commit message: `Add buy-stock rail: Alpaca brokerage + on-chain xStocks`

- 2026-06-13 11:03:47 +0700 - Fix Brain View Empty Boot Root Cause
  - Status: Uncommitted
  - Areas changed: dashboard state hydration (`src/lib/services/dashboard-state-client.ts`, `scripts/test-dashboard-state-snapshot.mjs`), Brain graph API/native builders (`src/lib/services/obsidian/brain-graph.ts`, `src-tauri/src/brain.rs`), Brain graph refresh and inspector UI (`src/features/dashboard/hooks/use-miroshark-brain-controller.tsx`, `src/features/dashboard/views/BrainGraphExplorer.tsx`)
  - Summary: Fixes the Brain view empty-boot root cause: an expired or missing dashboard auth session could make `/api/dashboard/state` return `401`, and the client treated that auth denial as a legitimate empty dashboard snapshot. That let an already-mounted dashboard hydrate into a fake-empty Brain state with 0 notes instead of returning to the unlock flow. The snapshot loader now treats auth denial as auth denial, never `{}`, and a regression test locks that behavior. As secondary guardrails, the Brain graph loader refuses to accept an empty native graph without trying the API graph builder, both graph builders now emit one edge per source/target pair, the inspector dedupes linked-node lists before rendering buttons, and the native scanner reports folder read failures instead of silently turning them into an empty graph.
  - Verification: `pnpm test:dashboard-state-snapshot` passed; `cargo check` passed for the Tauri native scanner; `pnpm exec eslint src/lib/services/dashboard-state-client.ts src/lib/services/obsidian/brain-graph.ts src/features/dashboard/views/BrainGraphExplorer.tsx src/features/dashboard/hooks/use-miroshark-brain-controller.tsx` reports no errors, only pre-existing hook dependency warnings in the large controller; live `localhost:5121` checks show `/api/dashboard/state` returns `401` when unauthenticated while `/api/obsidian/graph` can still return the populated vault graph (`267` nodes and `11` deduped links, no duplicate edge pairs), confirming the graph endpoint was not the empty-boot root failure and the duplicate React key warning path is removed. Full `pnpm typecheck` is blocked by an unrelated existing Remotion error in `remotion/gitlawb-integration-video/src/GitLawbIntegrationVideo.tsx` (`pathLength` is not a valid CSS property).
  - Intended commit message: `Fix Brain view auth-denied empty boot`

## 2026-06-13 10:52:28 +0700 - Fix Stuck Dashboard Loading Screen And Add Issue Reporting

- Status: Pushed
- Areas changed: dashboard hydration loader (`src/lib/services/dashboard-state-client.ts`, `src/features/dashboard/DashboardApp.tsx`), new client issue reporter (`src/lib/utils/issue-reporter.ts`), new Cloudflare Worker (`workers/issues/`)
- Summary: Fixes the bug where a fresh install could sit forever on a loading screen ("Scanning fleet discovery" on the desktop, "Opening Agent Chat" on mobile). Both screens gate on dashboard hydration, which only completed after the local-state snapshot loaded — and that loader retried `/api/dashboard/state` indefinitely with no give-up, so any persistent backend failure left the app on an endless spinner. The retry is preserved (it protects stored chat history from an empty-boot overwrite), but after ~10s of failure the app now shows a recoverable "Still connecting to HivemindOS…" screen with a Reload button that clears itself the moment the service answers. Broken boots are also reported (anonymized: failure kind, HTTP status, app version, coarse UA, random per-install id — never user content, paths, secrets, or IPs) to a new `hivemindos-issues` Cloudflare Worker + D1, so a stuck install is visible centrally without asking the user to open devtools.
- Verification: `tsc`/`eslint` clean on all changed files; `pnpm test:dashboard-state-snapshot` green (no-data-loss semantics preserved); issues Worker deployed and round-trip verified live (POST accepted, token-gated read works, unauthorized → 401). The loading screen itself is Tauri-only and needs a desktop build for a live look.
- Intended commit message: `Fix dashboard loading-screen hang; add anonymized issue reporting`

## 2026-06-13 10:52:28 +0700 - Fix Stuck Dashboard Loading Screen And Add Issue Reporting

- Status: Pushed
- Areas changed: dashboard hydration loader (`src/lib/services/dashboard-state-client.ts`, `src/features/dashboard/DashboardApp.tsx`), new client issue reporter (`src/lib/utils/issue-reporter.ts`), new Cloudflare Worker (`workers/issues/`)
- Summary: Fixes the bug where a fresh install could sit forever on a loading screen ("Scanning fleet discovery" on the desktop, "Opening Agent Chat" on mobile). Both screens gate on dashboard hydration, which only completed after the local-state snapshot loaded — and that loader retried `/api/dashboard/state` indefinitely with no give-up, so any persistent backend failure left the app on an endless spinner. The retry is preserved (it protects stored chat history from an empty-boot overwrite), but after ~10s of failure the app now shows a recoverable "Still connecting to HivemindOS…" screen with a Reload button that clears itself the moment the service answers. Broken boots are also reported (anonymized: failure kind, HTTP status, app version, coarse UA, random per-install id — never user content, paths, secrets, or IPs) to a new `hivemindos-issues` Cloudflare Worker + D1, so a stuck install is visible centrally without asking the user to open devtools.
- Verification: `tsc`/`eslint` clean on all changed files; `pnpm test:dashboard-state-snapshot` green (no-data-loss semantics preserved); issues Worker deployed and round-trip verified live (POST accepted, token-gated read works, unauthorized → 401). The loading screen itself is Tauri-only and needs a desktop build for a live look.
- Intended commit message: `Fix dashboard loading-screen hang; add anonymized issue reporting`
